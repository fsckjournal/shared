---
name: artist-identity-anchor
description: v4 artist identity is already ID-anchored & 100% covered — the "artist collapse" is a small folder-bridge job, not a 58-cluster merge slog
metadata:
  type: project
---

The v4 `artist` table (`music_v4.db`) is **already the canonical artist identity store** and does not need a "collapse."

**Verified 2026-07-09** `[verified: sqlite3 music_v4.db]`:
- `artist`: 13,326 rows. **100% external-ID coverage** — 0 rows with no ID. Spotify ID on 12,795, Beatport ID on 6,470, *some* ID on all 13,326.
- Columns: `id` (UUID PK), `name`, `beatport_artist_id` UNIQUE, `spotify_artist_id` UNIQUE.
- Andrew Bird case: **already two distinct rows**, BP 163487 vs 445546, distinct Spotify IDs — correctly split with no operator intervention.
- **0** junk-named rows (`_UNRESOLVED`/`all_tags`) and **0** near-duplicate name clusters in the `artist` table.

**Why:** slut's whole purpose is keying on external IDs, not name strings. Name is a display label, never a key. Keying on ID auto-resolves both proven hazards: over-merge (distinct people w/ similar names stay split) and annihilation ([^a-z0-9] norm erasing non-Latin names — فيروز/松下誠/!!! — never fires because the ID is the key).

**How to apply:** The "702 junk artists / 58 surface-variant clusters" from spine #114 live on the **MASTER_LIBRARY folder-name / drain-seeding surface**, NOT the identity store (two-store disease). So the artist-identity work is NOT a 58-cluster manual merge. It is a smaller bridge: map dirty folder-name strings → existing clean `artist.id`, drop 702 `_UNRESOLVED`/`all_tags` folders as non-artists, backfill from the ID anchor where names resolve, and route only genuinely ID-less name variants (a small tail) to the operator. Doctrine: agreeing evidence auto-resolves, operator rules disagreements once. See [[move-dont-menu]]. Spine: #114 (superseded framing), #115 (Andrew Bird correction).

**BRIDGE BUILT 2026-07-10** `[verified: table artist_folder_bridge in music_v4.membership_wip.db; real v4 untouched]`. Method = **ID graph, not string-match**: folder = `track_file.path` segment0 → `track_artist(role=primary)` → `artist.id`; folder-name only *confirms* agreement, never keys identity (dodges both hazards). 6,923 disk folders reconcile: **name+id_agree 3,331** (clean resolved) · **collab 1,019** (432 multi-primary + 587 collab-string, auto-resolved to component ids) · **admin_junk 3** dropped. #114's 702 junk is **GONE from disk** (library intaked since #114: `_UNRESOLVED` now 33 children, `_UNRESOLVED_FROM_LIBRARY` absent, `all_tags` 1) — #114 framing fully superseded, no merge slog. The ~2,100 unresolved are **mechanical data gaps, not rulings**: `has_files_no_artist_link` 1,691 (need artist row/link) · `no_files_in_db` 439 (433 audio never imported = ingest gap, 6 empty) · `disagree_mislink` 264 (folder right, primary link WRONG, e.g. Betty Wright→Hot Toddy) · `disagree_variant_autofold` 50 (diacritic/punct). **Genuine operator-ruling residue = 126** (`disagree_genuine_ruling`), exported `slut/artist_bridge_genuine_tail.tsv` — and even those are mostly mislinks + The-prefix + missed-collab; true AKA rulings (2Pac/Makaveli, Aya Matsuura/松浦亜弥) are a few dozen. This is the **artist axis**, separate from the membership/pool-iceberg collapse. Spine #116.

**MECHANICAL RELINK PASS 2026-07-10** `[verified: --apply on music_v4.membership_wip.db; real v4 untouched; snapshot .pre_relink kept]`. Key correction to the earlier relink plan: **ISRC is NOT an independent arbiter — it IS the source of the current primary links** (`current_link == ref_track_artist ord0` for 125/125 genuine_ruling, 258/258 mislink, 49/49 variant, 572/572 collab). So re-deriving primaries from ISRC is a no-op; ZERO relink candidates at ord0. The real signal is **ord>0**: the `disagree_mislink` bucket is mostly REMIX SIGNATURE — folder/original artist billed at ord>0 beneath the remixer at ord0. All-ords check: mislink **237/264 folder=ord>0 RESOLVABLE**, 26 match-no-ord (operator), 1 no-isrc; genuine_ruling **0 resolvable** (125 match-no-ord); variant_fold **0 resolvable** (49 match-no-ord). Pass promoted the folder-matching artist to `primary` and demoted the ord0 artist to `remixer` (e.g. Ben Sidran/Clyde Stubblefield, The Drums/RAC, Aaron Childs/Channel Tres). RESULT: **name+id_agree 3331→3576 (+245)**; disagree_mislink 264→27; collab_relink 587→579. **Operator tail 176→176 (delta 0) — mechanically IRREDUCIBLE, proven all-ords.** Created ZERO orphans (tracks-no-primary 7300 before==after). Idempotent. **`has_files_no_artist_link` 1691 = 0% ISRC coverage = enrichment/ingest gap, NOT relinkable, NOT rulings.** Tail export now 203 rows: aka_or_va_ruling 126 + variant_fold_proposal 50 (single batch confirm per #115) + residual_mislink 27. Spine #118.

**DEMOTION CORRECTION (same day, advisor-caught).** First pass inferred `role='remixer'` purely from ord ordering (B billed above folder-artist A ⇒ B=remixer). That's ambiguous: ord>0 billing of the folder artist splits into REMIX (B=remixer, correct) vs FEATURE/MEMBER (B=real lead, demotion INVERTS the credit). Evidence check: of 1096 demoted rows only 861 had `canonical_mix_name`; the 180 unbacked were mostly real inversions — Eurythmics→Dave Stewart (×5, founding member!), Wings→Paul McCartney, The Pogues→Kirsty MacColl (duet co-lead), Django Reinhardt→Stéphane Grappelli, VA folders. FIX: gate demotion on remix evidence (`canonical_mix_name` OR remix/edit/dub/mix/version token in `canonical_title`); when unbacked, add A as primary but KEEP B primary (folder resolves as collab — correct, no false remixer claim). Re-ran from snapshot: demotions 1096→970, feature inversions healed (Eurythmics/Dave Stewart back to primary), evidence-backed remixes retained (The Drums/RAC=remixer), still ZERO orphans, folder resolution unchanged (+245). LESSON: ord order is NOT role evidence; `canonical_mix_name`/title is. Spine #119.

**APPLIED TO REAL v4 2026-07-10 (operator GO).** `[verified: apply_artist_full.py --apply on real music_v4.db; re-verified post-commit from a 2nd process]`. The artist axis is **LANDED, not staged**. `apply_artist_full.py` (mechanical #119/#120 + credit-verified operator tail #126, reconciles to reference `music_v4.tail_apply.db`) ran clean: baseline re-check `1d660dd3` matched real v4; plan +439/−159 net **+280**; post-apply `track_artist` sha=`64da87af` (==target), 43240→43520; artist +8/−1/~1; **0 fk-orphans; primary-less 7300→7299** (no regression). Independently re-confirmed: real v4 now 43520 rows, sha1 `64da87af`. Idempotent (re-run detects target-state, exit0). Spine #130. **Don't re-open the artist collapse — it's done.** Remaining artist-adjacent items are NOT rulings: alias-capture fork (#123/#125, operator to rule where aliases persist — leans v4 `artist_alias` table) and `has_files_no_artist_link` 1691 (ingest/enrichment gap, not relinkable). **Hash gotcha for future sessions:** `apply_artist_full.py`'s `ta_sha` is SHA-1 over concatenated columns; an ad-hoc SHA-256/`repr()` integrity probe yields a DIFFERENT digest by construction and must NOT be misread as drift — the script's own `ta_sha` is the only authoritative check.
