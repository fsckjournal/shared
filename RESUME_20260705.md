# Resume note — 2026-07-05 ~00:30 (session handoff)

Boot a fresh session by invoking **`resolve-from-the-record`** and reading
`shared/STATE.md` + `shared/handoffs/handoff.jsonl` entries **#54–#65** (this session).
This note is just the fast pointer.

## Background jobs running now (both resumable — safe to leave, safe to restart)

1. **Apple MU scan** — `hag/tools/run_mu_scan.py` (was PID 67794). Analyzes all 30,507
   identity-gated masters; writes `hag/tools/apple_mu_analyses/<content_sha256>.json`
   (identity-keyed → ingest is a direct seam join, no `audio_file` dependency).
   ~28h left @ ~18/min. **Resume:** `python3 run_mu_scan.py` (skips done). Log: `mu_scan.log`.

2. **Spotify audio-analysis fetch** — `hag/tools/fetch_automix_payloads.py`.
   Retry list: `~/Projects/tag/spotify_ids_retry.txt` (22,830 = net-error + never-tried;
   404-stubs and successes excluded). Output `hag/tools/automix_payloads/<spotify_id>.json`
   (3,704 done). **Resume:** `poetry run python fetch_automix_payloads.py ~/Projects/tag/spotify_ids_retry.txt --delay 1`
   — re-tap Spotify → Devices when the Connect session drops (~every 2k). Uses the librespot
   backdoor because Spotify deprecated the public audio-analysis API (2024-11-27).

## What this session locked in (spine #54–#65, all v4 writes snapshotted + integrity-ok)

- **ref_beatport layer legitimized** (#54→#57, migration 0019) + **backfilled** (#61):
  ref_bp_track 10,219→15,975; Beatport-linked gate ISRCs 9,978→**15,734**; store BPM for N4.
- **Spotify bridge ~closed** (#62/#64/#65): gate masters with spotify_id 18,866→**24,303**
  (~95% of 25,686 ISRC-gate). Anna's Archive offline parquet did 4,876 vs ~488 for all
  Soundiiz batches — offline ISRC→spotify_id (`/Volumes/ATTIC/kaggle-data/spotify-aa/tracks.parquet`)
  beats the manual round-trip. 1,367 still ID-less → 676 just fetched, rest not on Spotify.
- **MU coverage-gap audit** (#60): old 459 sidecars are 0/gate (dead); fix = scan masters
  (now running). `hag:docs/automix/MU_COVERAGE_GAP_audit_20260704.md`.
- **Roon bridge design** (#63): deterministic PATH crosswalk (Roon export `Path` → strip
  `/MASTER_LIBRARY/` → NFC → track_file.path → content_sha256), 93.8% join, identification-
  agnostic. Don't key on ISRC/MusicBrainz. `hag:docs/automix/ROON_BRIDGE_design_20260704.md`.

## Next real moves (not started)

1. **Ingest the two analysis layers → seam by `content_sha256`** (Apple MU + Echo Nest
   payloads). Both are identity-keyed now; ingest is a join. Role = validation-only per #38
   until #38-pt4 (matcher feature-space) resolves.
2. **Nature-gate ratification (#50)** — operator decision (reject rules, N4 >130bpm threshold,
   Lexicon-as-weak-signal, Beatport calibration go/no-go). Now materially stronger.
3. **v4 refresh / §15 unpark decision** — 1,988 MASTER_LIBRARY files + ~266 staging downloads
   not in v4; refreshing v4 from disk is the parked write (LEDGER §15 / spine #44). Also slut Q5.

## Notes
- Nature-gate pre-filter idea was dropped for the MU scan (operator: analyze everything, the
  pool gate keeps non-mixable out of "the soup" — analysis is cheap/harmless).
- Spotify Mix Beta screenshots kept as UX reference: `~/Projects/tag/ss_1..4.png`.
